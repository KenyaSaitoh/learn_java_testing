// H2DB
h2db {
    driverClass = "org.h2.Driver"
    hostName = "localhost"
    rootPassword = ""
    port = "3306"
    general {
        user = "sa"
        password = "sa"
        db = "TESTDB"
        baseUrl = "jdbc:h2:~"
        url = "${baseUrl}/${general.db}"
    }
}